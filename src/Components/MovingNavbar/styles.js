import { css } from "styled-components";

const styles = css`
  #navbar {
    padding: 1vw 2vw;
    .title {
      color: white;
    }
    .navlink {
      color: white;
    }
  }
  .active {
    text-decoration: underline;
  }
`;

export default styles;